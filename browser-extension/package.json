{
  "name": "glassbox-lite-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Explicit, privacy-minimized GlassBox Lite audits for selected or pasted text.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
